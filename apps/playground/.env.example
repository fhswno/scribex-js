# Mistral AI (optional — only needed if using the Mistral provider)
MISTRAL_API_KEY=your-mistral-api-key

# Ollama (optional — defaults shown, works out of the box with local Ollama)
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=gemma3:4b
